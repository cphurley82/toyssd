# Deprecated: Python tooling is now managed via uv and pyproject.toml
# This file is intentionally left as a stub for legacy references.
# Please run:
#   uv sync --all-extras
# and use tasks via:
#   uv run invoke <task>
